def get_indices_of_item_weights(weights, limit):
    cache = {}
    """
    YOUR CODE HERE
    """
    # result = []
    # for i in weights:
    #     if i not in cache:
    #         cache[i] = 0
    #     cache += 1
    # result = [item[0] for item in cache.items() if item[0] + item[0+1] == limit]
    #
    # return result
